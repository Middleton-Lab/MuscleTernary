# Calculate vector magnitude

Calculates the magnitude of a given vector

## Usage

``` r
mag(vector)
```

## Arguments

- vector:

  Vector whose magnitude you want to measure

## Value

Numeric: Magnitude of the vector (x, y, z).
