# Create a unit vector from a given vector

Create a unit vector from a given vector

## Usage

``` r
make_unit_vector(x)
```

## Arguments

- x:

  Vector of length 3.

## Value

A normalized unit vector of length 3
